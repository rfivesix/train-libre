<?xml version="1.0" encoding="utf-8"?>
<!--
  Taller than the others by default: the heatmap is the point of this widget and
  the compact layout only gives it a narrow column. Still resizable down to a
  4x2, where the side-by-side layout takes over.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_last_workout_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="4"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
